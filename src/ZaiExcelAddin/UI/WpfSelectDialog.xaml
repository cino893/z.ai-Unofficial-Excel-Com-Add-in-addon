<Window x:Class="ZaiExcelAddin.UI.WpfSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380" Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#F0F2F5"
        FontFamily="Segoe UI, Segoe UI Emoji">

    <Window.Resources>
        <LinearGradientBrush x:Key="GreenGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#217346" Offset="0"/>
            <GradientStop Color="#1D6B3F" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource GreenGradient}">
            <TextBlock x:Name="lblTitle" Text="Select" FontSize="16" FontWeight="SemiBold"
                       Foreground="White" VerticalAlignment="Center" Margin="16,0"
                       FontFamily="Segoe UI, Segoe UI Emoji"/>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="20,16,20,16" VerticalAlignment="Center">
            <TextBlock x:Name="lblPrompt" Text="Choose an option:" FontSize="13"
                       Foreground="#1C1E21" Margin="0,0,0,10" TextWrapping="Wrap"
                       FontFamily="Segoe UI, Segoe UI Emoji"/>

            <!-- ComboBox -->
            <Border CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1"
                    Background="White" Margin="0,0,0,20">
                <ComboBox x:Name="cboItems" FontSize="13" Padding="8,6"
                          Background="White" BorderThickness="0"
                          FontFamily="Segoe UI, Segoe UI Emoji"/>
            </Border>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnOk" Content="OK" Width="90" Height="34" Margin="0,0,8,0"
                        FontSize="13" FontWeight="SemiBold" Foreground="White" Cursor="Hand"
                        Click="OnOkClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" CornerRadius="6" Background="{StaticResource GreenGradient}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#2E8B57"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="btnCancel" Content="Cancel" Width="90" Height="34"
                        FontSize="13" Foreground="#4B5563" Cursor="Hand"
                        Click="OnCancelClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" CornerRadius="6" Background="#E5E7EB"
                                    BorderBrush="#D1D5DB" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#D1D5DB"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
